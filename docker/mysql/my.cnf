[mysqld]
# 文字コードをutf8mb4に設定
# utf8mb4はUTF-8の完全な実装で、絵文字や特殊文字も保存できます
# (MySQLの"utf8"は3バイトまでしか対応していないため注意)
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci

# タイムゾーンをJSTに設定
default-time-zone = '+09:00'

[client]
default-character-set = utf8mb4
